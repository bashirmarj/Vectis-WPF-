# Production requirements for Vectis Geometry Service
# Version 2.0.0 - BRepNet + Geometric Fallback

# Core web framework
flask==3.1.0
flask-cors==4.0.0
gunicorn==21.2.0

# Database client
supabase==2.7.4

# ONNX Runtime for ML inference (CPU-optimized)
onnxruntime==1.16.3

# PyTorch (required for ONNX model loading utilities)
torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu

# Scientific computing
numpy==1.24.4

# Graph operations (if needed for B-Rep graph construction)
networkx==3.2.1

# Monitoring
psutil==5.9.6

# OpenCascade Python bindings (pythonocc-core)
# NOTE: Must be installed via conda or system package manager
# Installation instructions in DEPLOYMENT.md
