# Core web framework
flask==3.1.0
flask-cors==4.0.0
gunicorn==21.2.0

# Database client
supabase==2.7.4

# ML dependencies (PyTorch installed separately in Dockerfile)
pytorch-lightning==2.1.0
onnxruntime==1.16.3

# Scientific computing
numpy==1.24.4

# Graph operations
networkx==3.2.1

# NOTE: occwl is a local folder in geometry-service, not a pip package

# Monitoring
psutil==5.9.6

# OCCWL dependency
deprecate==0.7.0

# NOTE: torch installed separately in Dockerfile (CPU-only for smaller size)
# NOTE: pythonocc-core installed via conda
# NOTE: occwl is a local folder, imported via PYTHONPATH
